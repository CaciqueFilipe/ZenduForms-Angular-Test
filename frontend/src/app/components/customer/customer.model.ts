export interface Customer {
    id?: number;
    check: boolean;
    companyName: string;
    customerAddress: string;
    contact: string;
    phoneNumber: string;
    openTasks: number;
    totalCompleted: number;
}